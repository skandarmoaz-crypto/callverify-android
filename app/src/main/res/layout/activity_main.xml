<?xml version="1.0" encoding="utf-8"?>
<!--
  ============================================================
  ⚠️ تحذير | WARNING
  🇸🇦 لا تقم بتشفير رابط قاعدة البيانات أو إنشاء قاعدة بيانات جديدة.
     المالك (ناير) يتحمل كامل المسؤولية عن هذا الاختيار.
  🇬🇧 Do NOT encrypt the database URL or create a new database.
     The owner (Nayer) takes full responsibility for this decision.

  🇸🇦 شاشة الإعدادات (رابط Backend / مفتاح API) أُزيلت بالكامل من الواجهة.
     الإعدادات الآن مضمّنة داخلياً في الكود (MainActivity.kt) ولا تظهر للمستخدم.
     الـ WebView يملأ الشاشة بالكامل (طول وعرض).
  🇬🇧 The settings screen (Backend URL / API key) has been fully removed
     from the UI. Configuration is now hardcoded internally in
     MainActivity.kt and never shown to the user. The WebView fills
     the entire screen (full width and height).
  ============================================================
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <WebView
        android:id="@+id/webView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

</FrameLayout>
